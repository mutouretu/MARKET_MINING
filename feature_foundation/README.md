# feature_foundation

Phase 2 physical layer for macro features, liquidity features, stablecoin features, and BTC transmission features.

This layer consumes data foundation outputs. It must not call external source APIs directly or render UI.
