# analysis

Physical subproject for feature engineering, scoring, regimes, positioning, and backtest statistics.

This layer consumes data mining outputs. It must not call external source APIs directly or render UI.
